export interface SharedDetails {
    firstName: string;
    lastName: string;
    isAdmin: boolean;
    isLoggedIn: boolean;
    isLoginPage: boolean;
}
export interface LoginDetails {
  logValue: boolean;
}
